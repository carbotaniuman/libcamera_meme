{
  "tasks": [
    {
      "label": "Build libcamera_meme",
      "type": "shell",
      "command": "cmake",
      "args": [
        "--build",
        "build",
        "--target",
        "libcamera_meme"
      ],
      "options": {
        "cwd": "${workspaceFolder}/"
      },
      "group": {
        "kind": "build",
        "isDefault": true
      }
    },
  ]
}